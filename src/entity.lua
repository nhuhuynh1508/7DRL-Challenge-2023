local Entity = Class('Entity')

function Entity:initialize()
end

function Entity:update(dt)
end

function Entity:draw()
end

function Entity:mousepressed(x, y, button)
end

function Entity:mousemoved(x, y)
end

function Entity:mousereleased(x, y, button)
end

function Entity:keypressed(key, scancode, isRepeat)
end

function Entity:keyreleased(key, scancode, isRepeat)
end

return Entity